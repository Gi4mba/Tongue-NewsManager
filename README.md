# Tongue-NewsManager
mart contract commisioned to reward "validator", fact-checking news users. It keeps track of: modifiable "validators" users list, rewards amount for each and total assigned token, contract balance, news validation rules, validators reputational system hence a supportive library for contract interaction purposes.
